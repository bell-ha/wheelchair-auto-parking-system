# 젯슨 나노 배포용 파이썬 의존성
#
# 주의(젯슨): opencv는 pip로 설치하면 GStreamer 미지원 빌드가 깔림.
# CSI 카메라(--csi)뿐 아니라 일부 USB 웹캠(예: C920)도 pip 빌드의
# V4L2 백엔드에서 select() 타임아웃으로 무한 대기하는 문제가 있었음
# (카메라/커널 조합에 따라 다를 수 있음) -> live_camera.py는 USB/CSI
# 모두 GStreamer 파이프라인을 씀. JetPack 기본 포함 OpenCV(dist-packages,
# pip opencv-python 미설치 상태)를 쓸 것: pip3 uninstall opencv-python
ultralytics

# hardware/ultrasound/keyboard.py (수동 조종 키보드 → ESP32 USB 시리얼)
pyserial
pynput
